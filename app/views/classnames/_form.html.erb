<%= form_for @classname do |f| %>

<% if @classname.errors.any? %>
    <div style = "color:blue;">
      <h2>
        <%= pluralize(@classname.errors.count, "error") %> prohibited this classname from being saved:
      </h2>
      <ul>
        <% @classname.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>
  
  <p>
    <%= f.submit class:"btn btn-xs btn-danger" %>
  </p>
 
<% end %>